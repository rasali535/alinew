{
  "name": "alinew-production",
  "version": "1.0.0",
  "description": "Production entry point for Alinew",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd frontend && npm install && npm run build && cd .. && node move-build.js",
    "test": "echo \"No tests specified\" && exit 0",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemailer": "^6.9.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}